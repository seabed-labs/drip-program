name: Test
on:
  pull_request:
  push: { branches: main }

jobs:
  test:
    name: Run test suite
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Prepare
      id: prep
      run: |
        TAG=$(echo $GITHUB_SHA | head -c7)
        IMAGE="dca_test_image"
        echo ::set-output name=tagged_image::${IMAGE}:${TAG}
        echo ::set-output name=tag::${TAG}
  
    - name: Set up Docker Buildx
      id: buildx        
      uses: docker/setup-buildx-action@master

    - name: Cache Docker layers
      uses: actions/cache@v2
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-single-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-single-buildx

    - name: Build test image
      uses: docker/build-push-action@v2
      with:
        context: .
        builder: ${{ steps.buildx.outputs.name }}
        file: ./build/githubactions/dockerfile
        load: true
        push: false # This would be set to true in a real world deployment scenario.
        tags: ${{ steps.prep.outputs.tagged_image }}
        cache-from: type=local,src=/tmp/.buildx-cache
        cache-to: type=local,dest=/tmp/.buildx-cache-new

    - name: Run unit tests
      run: docker run --rm ${{ steps.prep.outputs.tagged_image }} bash ./scripts/test.sh

    - name: Move cache
      run: |
        rm -rf /tmp/.buildx-cache
        mv /tmp/.buildx-cache-new /tmp/.buildx-cache

    # - name: Build and run docker images
    #   run: docker-compose --file ./build/githubactions/docker-compose.yaml up -d --build

    # - name: Run unit tests
    #   run: docker exec githubactions_test_1 bash ./scripts/test.sh

    # - name: Stop Docker
    #   run: docker-compose --file ./build/githubactions/docker-compose.yaml down
