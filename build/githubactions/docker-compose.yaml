version: '3.7'
services:
  test:
    build:
      context: ../../
      dockerfile: build/githubactions/dockerfile
    command: tail -F anything
  # integration-test:
  #   build:
  #     context: ../../
  #     dockerfile: build/githubactions/dockerfile
  #   command: anchor test --provider.wallet /root/.config/solana/id.json || exit $$?
